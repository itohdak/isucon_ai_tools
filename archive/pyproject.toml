[project]
name = "isucon-ai-tools"
version = "0.1.0"
description = "AI-assisted ISUCON operation helpers built around Skills and MCP."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "PyYAML>=6.0.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
