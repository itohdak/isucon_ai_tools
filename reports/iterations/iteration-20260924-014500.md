# Iteration 20260924-014500 — Second-session iteration 4: MySQL performance_schema=OFF + 1G buffer pool/redo (neutral, kept)

## Agents Used

| Agent | Used | Output |
| --- | --- | --- |
| Profiler / Resource Monitor / App Understanding / SQL | reused from iteration 2's dispatch | s2 CPU-bound, s1 headroom (at that time). |
| Implementer / Verifier | no (Codex directly) | 3-line config change; tightly coupled to deploy_db.sh + bench. |
| Recorder | yes | this report |

## Hypothesis
After iteration 3, remove remaining per-statement MySQL overhead on the CPU-bound s2: `performance_schema` instrumentation (nothing reads it; slow log + SHOW STATUS are the observability sources), and give InnoDB non-stock buffer/redo sizes (1G/1G on the 3.6GB host).

## Evidence
Iteration 3 baseline 29302. Live values before: `performance_schema=1`, buffer pool 128M, redo 100M.

## Change
`common/etc/mysql/mysql.conf.d/mysqld.cnf`: `performance_schema = OFF`, `innodb_buffer_pool_size = 1G`, `innodb_redo_log_capacity = 1G`. Slow log untouched (verified `slow_query_log=1`, `long_query_time=0` live after deploy).

## Verification
deploy_db.sh, variables verified live, 3 runs all pass=true.

## Benchmark Result
28215 / 29816 / 29011 (avg 29014) vs iteration 3's 29302 -- within noise (+/- ~1000). Kept (harmless, removes instrumentation overhead), not claimed as a win. A separate single bench during a netdata capture scored 28951, and another 29907.

## pprotein artifacts
`dln5knotuew8-pprof.pb.gz`, `dln5knotws49-httplog.log`, `dln5knotxsbz-slowlog.log` (s3 `/home/isucon/data/`).

## Commit
`a3386ee`.

## Next candidate
Netdata capture in the steady state of this run shows BOTH hosts near saturation now: s2 ~100% busy (user 71 + sys 19 + softirq 9), s1 ~87% (user 55 + sys 20 + softirq 12). pprotein's pprof for the run shows `crypto/sha256` = 35.6% of the app's CPU (19.9s of 56s) from `fillUserResponse` hashing the full icon BLOB on every call -> iteration 5.
