# Iteration 2026-09-21 17:28:42 JST

## Summary

- Change: cache per-chair `total_distance` on `chairs` and update it in `POST /api/chair/coordinate`.
- Commit: `40f918b cache chair total distance`
- Deployment: s1 `/home/isucon`, pushed to `git@github.com:itohdak/isucon14_practice.git` `main`.
- Result: `pass=true`, score `5152`, errors `map[]`.

## Motivation

The previous run on `03bbced` scored `5101` with `map[26:1]`.
`CODE=26` means `/api/owner/chairs` returned stale `total_distance`.
The endpoint also spent time recomputing chair distance from the full `chair_locations` history with a window function.

## Implementation

- Added `total_distance` and `total_distance_updated_at` to `chairs` after initial data import in `webapp/sql/init.sh`.
- Backfilled those columns from existing `chair_locations` during initialize.
- Updated `POST /api/chair/coordinate` to:
  - read the previous chair location,
  - insert the new location,
  - increment cached `chairs.total_distance`,
  - update `total_distance_updated_at`,
  - preserve `chairs.updated_at` with `updated_at = updated_at`.
- Changed `GET /api/owner/chairs` to read cached values instead of recomputing with a window query.
- Added the new columns to the Go `Chair` model so existing `SELECT * FROM chairs` scans remain valid.

## Verification

- `go test ./...`: passed.
- `webapp/sql/init.sh`: passed after moving column addition from `1-schema.sql` to post-import `ALTER TABLE`.
- Services after deploy: `isuride-go`, `isuride-matcher`, `isuride-payment_mock`, `nginx`, `mysql` all active.
- Benchmark:
  - previous: `5101`, errors `map[26:1]`
  - current: `5152`, errors `map[]`

## pprotein Artifacts

- pprotein group time: around `2026-09-21 08:28 UTC`
- httplog: `/home/isucon/data/dlkuouqizamp-httplog.log`
- slowlog: `/home/isucon/data/dlkuouqior81-slowlog.log`
- pprof: `/home/isucon/data/dlkuouqivcwh-pprof.pb.gz`

## Latest Totals

Top API by total request time:

| route | count | total reqtime |
|---|---:|---:|
| GET `/api/chair/notification` | 19071 | 1168.958s |
| GET `/api/app/notification` | 8850 | 810.772s |
| POST `/api/chair/coordinate` | 2795 | 296.702s |
| GET `/api/app/nearby-chairs` | 105 | 113.232s |
| GET `/api/owner/sales` | 142 | 25.430s |
| GET `/api/owner/chairs` | 166 | 5.488s |

Notable SQL by total query time:

| query | count | total |
|---|---:|---:|
| `getLatestRideStatus` latest status lookup | 23543 | 20.017s |
| latest ride by chair, updated_at | 21881 | 16.276s |
| chair auth by access token | 22104 | 12.807s |
| chair notification unsent statuses | 9798 | 8.513s |
| app notification unsent statuses | 8856 | 7.777s |
| previous location lookup for distance cache | 5056 | 5.571s |
| cached owner chairs read | 166 | 0.173s |

## Notes

- The first attempt failed during benchmark prepare because adding columns directly to `1-schema.sql` broke initial data imports that use implicit column order.
- The fix keeps `1-schema.sql` compatible and adds/backfills cache columns after the initial import.
- `CODE=26` disappeared in the verified run.

## Next Candidate

The remaining dominant cost is notification polling and repeated status/latest-ride lookups.
Next work should target derived latest-ride/latest-status state or reducing repeated notification SQL, with extra care because notification ordering is regulation-sensitive.
