# Iteration 20260924-040500 — Second-session iteration 10: livestream tag-list/tags-table cache (REJECTED, reverted) and iteration 11: echo Debug=false (ACCEPTED)

## Agents Used

| Agent | Used | Output |
| --- | --- | --- |
| Profiler Agent | yes | Fresh run at `8923b12` (score 131576; artifacts `dln6gfks3t0i-httplog.log`, `dln6gfks5eq1-slowlog.log`, `dln6gfks91mz-pprof.pb.gz`): icon route now 61.7k calls at 1ms avg (61,495 are 304s: the bench does send `If-None-Match`); top routes search 155s, POST livecomment 67s, GET reaction/livecomment 51s each; isupipe SQL total 63.5s over 401k queries; top shapes `livestream_tags WHERE livestream_id` 105k/13.5s, `tags IN` 97k/10.8s, livestream stats aggregate 57 calls/8.2s, user stats aggregate 65/6.7s; isudns 120s (untouched). pprof: `Syscall6` 25%, `fillLivestreamResponse` 7.3s (91% in tag SELECTs), `json.appendIndent` 2.44s = 5.3% flat, `verifyUserSession` cookie decode 7.9%. |
| App Understanding Agent | yes | Mutation ground truth: `tags` static, `livestreams` and `livestream_tags` insert-only (reserve only); mutable: livecomments (moderation DELETE), viewers history (enter/exit), ng_words/reactions/reports append-only. Ranked LOW: cache livestreams/tags/tag lists; drop tx on read-only handlers; reuse loaded rows in post handlers; JOIN aggregates for user stats. MEDIUM: NG-word LIKE semantics (collation), rank in SQL. HIGH: in-memory counters. |
| SQL Agent | reused iteration 8's | -- |
| Resource Monitor Agent | no (Codex read netdata) | s1 ~93% busy (user 57 + sys 27 + softirq 9), s2 ~99% (user 81 + sys 13 + softirq 5); both saturated. |
| Implementer / Verifier | no (Codex directly) | Small changes; A/B needs tight live control. |
| Recorder | yes | this report |

## Iteration 10 — tag cache (REJECTED)
Hypothesis: cache per-livestream tag lists and the static `tags` table (`getLivestreamTags`, `tagTableCache`, `livestreamTagsCache`, cleared by `clearUserCaches`) to remove ~200k queries (24s DB time) per window. Functionally verified (`/api/livestream/search?tag=DIY` -> 170 rows, tags populated, ids descending).
Benchmark: commit `4588777`: 120746 / 118534 / 118748 / 117765 (avg 118.9k, 4 runs) vs A/B control `8923b12` (live revert): 130727 / 127946 / 127147 (plus earlier 127447 / 126518 / 129294 / 131576) = 7 runs avg 128.7k. Consistent ~-7.6% regression. Bench logs show different scenario dynamics (e.g. streamer-moderate 236 vs 59 successes, viewer-spam 360 vs 105, cold-reserve failures 321 vs 1008; DNSAttacker parallelism 3 in both), i.e. the closed-loop benchmark reacts to the faster read path by shifting work between scenarios, and revenue fell. Same family of outcome as session 1's iteration 2 and session 2's earlier icon_hash regression: a further app-side speed-up is not automatically a score gain. Reverted with a normal revert commit `0236454` (app code only; no observability settings touched); re-confirmed 127425 / 126363 afterwards.

## Iteration 11 — `e.Debug = false` (ACCEPTED)
Hypothesis: `e.Debug = true` makes echo indent every JSON response (`json.appendIndent` = 5.3% of app CPU, plus larger bodies). Response whitespace is not part of the API contract. Change: `webapp/go/main.go` `e.Debug = false`. Side effect: for non-HTTPError errors echo returns the generic "Internal Server Error" instead of the error string (bodies of 5xx are not checked by the bench; status codes unchanged).
Benchmark: commit `9430c14`: 136587 / 131115 / 134536 (avg **134079**) vs control ~128k (9 runs at `8923b12`-equivalent code) => ~+4.5%.

## Commits
`4588777` (tag cache), `0236454` (its revert), `9430c14` (Debug=false; current HEAD of this iteration).

## Next candidate
Per-request cost now dominated by DB round trips (`Syscall6` 25%): transactions on read-only handlers (BEGIN+COMMIT = 2 extra round trips each; ~40k START TRANSACTION/COMMIT per window), user-stats per-livestream loops -> JOIN aggregates, gorilla session-cookie decode (`verifyUserSession` 7.9%), echo Logger middleware (journald writes). Remember the tag-cache lesson: A/B every step.
