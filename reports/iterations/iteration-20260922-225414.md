# Iteration 2026-09-22 22:54:14

## Agents Used

- Sub-agents were not used because: this was a direct retry of a previously-analyzed candidate (the `FOR SHARE` removal from `reports/iterations/iteration-20260922-211313.md`), now that its earlier correctness objection had been independently root-caused and fixed (`9179ded`, previous iteration). The task was to re-verify with fresh benchmark evidence, which required tight loop with live benchmark feedback.

## Hypothesis

With the `CODE=15` root cause fixed independently (matching no longer reassigns a chair before it has received its previous ride's COMPLETED notification), the earlier `FOR SHARE` removal on the `users` read in `chairGetNotification` should now be safe to retry, since it was very likely never the true cause of that failure — only a throughput nudge that made the pre-existing bug marginally more likely to surface.

## Change

- Re-applied the same one-line change reverted in `085609b`: removed `FOR SHARE` from the `users` lookup in `chairGetNotification`. Commit `150e043 retry: remove unnecessary FOR SHARE lock on immutable user read`.

## Verification

- Local build/vet: clean. Deployed to `s1`, all services active.
- 4 consecutive benchmark runs, all `pass=true`, all `errors=map[]`, **no recurrence of `CODE=15`** (confirming the matching-side fix holds even with this lock removed):

| Commit | Run | Score | Pass | Errors |
| --- | --- | --- | --- | --- |
| `150e043` (FOR SHARE removed) | 1 | 5067 | true | `map[]` |
| `150e043` | 2 | 4943 | true | `map[]` |
| `150e043` | 3 | 5024 | true | `map[]` |
| `150e043` | 4 | 4964 | true | `map[]` |

- Average ≈4999.5, noticeably below the fix-only baseline's average (~5125 across 5 runs in the previous iteration). To rule out environmental drift (as done twice earlier today), immediately redeployed the fix-only commit (`9179ded`, `FOR SHARE` still present) and re-measured twice under the same current conditions:

| Commit | Run | Score | Pass | Errors |
| --- | --- | --- | --- | --- |
| `9179ded` (FOR SHARE present, A/B) | 1 | 5097 | true | `map[]` |
| `9179ded` | 2 | 5137 | true | `map[]` |

- This A/B pair (avg 5117) lines up closely with `9179ded`'s earlier 5-run average (~5125) and sits consistently above all 4 of `150e043`'s runs, with only marginal overlap at the boundary (5067 vs 5097). Unlike the score-neutral cases seen earlier today, this gap held up under a same-conditions A/B check rather than dissolving into it.

## Decision

**Reject (again), for a different reason than before.** The correctness issue is resolved, but the performance rationale for this change does not hold up under re-measurement — if anything, removing the lock now looks mildly *worse* for score, not neutral-to-positive as originally reasoned from query cost alone. Reverted via `git revert 150e043` (commit `35c1e70`), pushed, and redeployed to `s1`. Final confirmation run: `pass=true`, score `5232`, `errors=map[]`.

No confirmed explanation for why `FOR SHARE` removal would cost score rather than save it — it may be a genuine (if counterintuitive) interaction with InnoDB's read-view/locking-read behavior under this specific access pattern, or it may just be a slightly unlucky sampling window despite the A/B check. Either way, the evidence does not support keeping the change, so per the project's evidence rules it is rejected.

## Commit

- App commit (final/deployed): `35c1e70 Revert "retry: remove unnecessary FOR SHARE lock on immutable user read"` on top of `9179ded`.
- Repository: `git@github.com:itohdak/isucon14_practice.git`
- Pushed: yes
- App host `s1` is on `main` at `35c1e70`, clean, all services active, confirmed passing.

## Next Candidate

- **Do not retry this specific `FOR SHARE` removal a third time** without a concrete new theory for the measured score cost — two independent rejections (one for correctness, one for performance) is enough signal to leave this one alone.
- Return to other identified candidates: `/api/owner/sales`'s `updated_at BETWEEN` join query (owner_handlers.go:125), or investigate whether `chairGetNotification`'s "most recently updated ride" selection strategy itself deserves a more robust rewrite as defense-in-depth alongside the matching-side fix.
