# Iteration (ISUCON13, third session, 5 of 5): nginx buffering and keepalive_requests -- ACCEPTED, +2.7%

## Agents Used

| Agent | Used | Output |
| --- | --- | --- |
| Profiler / Resource Monitor | reused from iteration 4 (evidence one iteration old, same topology) | nginx ~52% of a core on s1 (90% busy host), softirq 8.7%; s4 bench 91% busy. |
| Local evidence | direct | 19,424 of 199,989 responses in the access log were >32 KB (livecomment/reaction lists, search) and the tracked nginx used the default 36 KB proxy buffers, so ~10% of responses were spooled through temp files (error.log is at `error` level, so the `buffered to a temporary file` warning is invisible); nginx 1.18's `keepalive_requests` default is 100 (both client and upstream); the access log did one write() per request. |
| App Understanding / SQL / Implementer / Verifier / Recorder | no (config-only change with deterministic effect; Codex did it directly) | - |

## Change (commit 879f628; only `common/etc/nginx/nginx.conf` and `sites-enabled/isupipe.conf`)

`access_log ... ltsv buffer=64k flush=1s` (log stays complete; delayed by at most 1 s, LTSV format unchanged, `/api/initialize` truncation unaffected), `keepalive_requests 100000` (http and upstream), `proxy_buffer_size 16k; proxy_buffers 32 8k`.

## Benchmark result (all pass=true)

250489 / 248390 / 225777 / 244840 / 247886 / 244373 -> mean **243.6k** vs 237.1k for the previous commit (5 of 6 runs above its best run, one low outlier 225777 with viewer 1161). Error map unchanged.

## Commit

879f628, pushed to `isucon13_practice_3` main.

## Next candidate

`moderateHandler` collation correctness fix; register's `pdnsutil` fork/exec; search (do not speed up naively).
