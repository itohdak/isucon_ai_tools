# Iteration 2026-09-23 00:46:04

## Agents Used

- Sub-agents were not used because: this was a direct, much smaller retry of the immediately-preceding rejected experiment (`reports/iterations/iteration-20260923-001626.md`), requiring tight loop with live benchmark feedback to validate the incremental approach.

## Hypothesis

The previous attempt (loop matching up to 20 rides per `internalGetMatching` call) caused a catastrophic system-wide overload. Rather than abandoning the approach, retry with a much smaller step — `maxMatchesPerCall = 3` instead of 20 — to increase matching throughput gradually and check whether a modest increase avoids the overload signature while still relieving the `CODE=32` matching-latency issue.

## Change

`webapp/go/internal_handlers.go`: same loop-based refactor as the rejected attempt, but `maxMatchesPerCall = 3`. Commit `000d3fc`.

## Verification

- Local build/vet: clean. Deployed to `s1` (`HOSTNAME=s1 deploy.sh` workaround), all services active, local `mysql.service` stopped.
- 5 consecutive benchmark runs:

| Run | Score | Pass | Errors |
| --- | --- | --- | --- |
| 1 | 12927 | true | `map[17:1]` |
| 2 | 10595 | true | `map[]` |
| 3 | 12519 | true | `map[]` |
| 4 | 12684 | true | `map[17:1]` |
| 5 | 12667 | true | `map[17:1 26:1]` |

- All 5 runs passed. Average ≈12278 — roughly **2.2x** the pre-fix post-split average (~5477 from the previous iteration). Matching-latency dissatisfaction dropped from a steady ~95% across this whole session to 47-84% depending on the run (still meaningfully unsatisfied on many runs, so there's more headroom, but no longer causing outright failures).
- Occasional minor `CODE=17` (2 of 5 runs, count 1 each) and one `CODE=26` (owner chair distance freshness — a pre-existing, previously-documented minor warning unrelated to this change). Neither caused `pass=false`; not investigated further this iteration since they don't threaten the benchmark result.
- Post-fix `pidstat` resource check: `s1`'s `isuride` process avg 70.47% CPU (similar headroom to the post-DB-split baseline). `s3`'s `mysqld` avg **169.47%** CPU (up from ~141% at the post-split baseline) — MySQL is now working substantially harder given ~2.2x more throughput is flowing through it, and is approaching its 2-vCPU ceiling (200%).

## Decision

**Accept.** A 3x step (vs. the rejected 20x) delivered a large, consistent, stable improvement (5/5 clean passes, ~2.2x average score) without reproducing the previous attempt's overload failure mode. This confirms the earlier hypothesis: the fixed one-match-per-tick rate really was suppressing overall system throughput far below what the (now DB-split) infrastructure can sustain, and a moderate increase — not a 20x jump — was the right-sized fix.

## Commit

- App commit: `000d3fc match up to 3 rides per matching tick as a smaller step than the rejected 20x attempt`
- Repository: `git@github.com:itohdak/isucon14_practice.git`
- Pushed: yes
- App host `s1` is on `main` at `000d3fc`, all services active, local `mysql.service` stopped.

## Next Candidate

- `s3`'s `mysqld` is now averaging ~169% CPU on a 2-vCPU host — noticeably closer to saturation than the ~141% seen right after the split. If further throughput increases are attempted (e.g., raising `maxMatchesPerCall` further, or other app-side speedups), watch `s3` resource metrics via the Resource Monitor Agent before assuming there's still headroom; `s3` itself could become the next bottleneck.
- Consider whether `maxMatchesPerCall` can be increased a bit further (e.g. 5-8) with the same careful incremental-and-measure approach, now that 3 is confirmed stable — but given `s3`'s rising CPU, verify DB headroom first rather than immediately retrying a bigger batch size.
- The occasional `CODE=17` warning (2 of 5 runs) hasn't been investigated; worth a quick look if it starts recurring more often or at higher counts.
- Investigate why matching-latency dissatisfaction is still 47-84% depending on the run (not yet near 0%) — there may be further gains available in matching frequency/logic once DB headroom is confirmed safe.
