# ISUCON13 (ISUPipe) — Running Summary

This is the chronological running summary for the ISUCON13 (ISUPipe) contest/project, per the convention described in `AGENTS.md`. It is the "read this first" index into the detailed evidence in `reports/iterations/` and `reports/history/` — append a new dated entry here whenever the overall status changes (a new accepted/rejected iteration, an environment change, a user-directed policy change), oldest first. Full environment facts live in `config/isucon13.yaml` and `AGENTS.md`'s "Current Environment Notes — ISUCON13" section; this file is a narrative log, not the source of truth for current facts.

This file replaces the old `MILESTONES.md` convention (that file is now frozen at `archive/MILESTONES.md`, not updated further — it also recorded the build-out of an earlier, now-abandoned Python MCP/orchestrator tooling layer, which has nothing to do with this ISUCON13 project).

---

- **New project started 2026-09-23**: ISUCON13's ISUPipe (livestreaming) app, a from-scratch environment separate from the ISUCON14 (ISURIDE) project (see `reports/summary/isucon14.md`). Full facts in `config/isucon13.yaml` and `AGENTS.md`'s "Current Environment Notes — ISUCON13" section.
- CloudFormation stack `isucon13` created from `isucon_cf_provisioning/isucon13/cf-template-isucon13.yaml` (profile `isucon15.prep`, region `ap-northeast-1`). `s1`=app (`54.95.240.51`/`192.168.0.11`), `s2`=spare/idle, kept infra-symmetric with `s1` (`13.192.67.176`/`192.168.0.12`), `s3`=bench+pprotein (`35.73.166.230`/`192.168.0.13`).
- pprotein + netdata deployed via `isucon_ansible` (`deploy_general.yaml`, `deploy_pprotein.yaml`) to all 3 hosts. Fixed two environment gaps found during setup: (1) the AMI's `pdns` apt repo has an expired GPG signing key, breaking `apt-get update` for the `general` role's `graphviz`/`netdata` install tasks — removed `/etc/apt/sources.list.d/pdns.list` on all 3 hosts (pdns itself is already installed from the AMI, not via apt, so this is safe); (2) MySQL's slow query log was off by default and `/var/log/mysql/` didn't exist, which silently broke pprotein slowlog collection (`failed to open: ... no such file or directory`) — enabled `slow_query_log=1`/`long_query_time=0` and created the log directory on both `s1` and `s2`.
- Git repo: `git@github.com:itohdak/isucon13_practice_3.git`, initial commit `c2b3fd9`, then `d5bd00d` (adds pprotein pprof integration + initialize collect hook, and fixes a `.gitignore` bug — see below). Both pushed successfully.
- **Found and fixed a `.gitignore` bug that silently dropped the entire Go application source from the first commit**: a bare `go/` line (copied from the isucon14_practice `.gitignore`, meant only to exclude `~/go`) is unanchored and matched `webapp/go/` too, so `git add -A` added zero `.go` files with no error or warning from `git status`. Caught by cross-checking `git ls-files webapp/go/` against `find webapp/go -type f` after noticing `git show HEAD:webapp/go/main.go` failed. Fixed by anchoring to `/go/`. Re-add and re-verify file counts explicitly any time a `.gitignore` is reused across ISUCON projects.
- pprotein collection quirk confirmed: the trigger endpoint is `GET /api/group/collect` (not POST — POST returns 404).
- Baseline benchmark (2026-09-23, commit `d5bd00d`): `pass=true`, `score=3371`. Command: `sudo -u isucon /home/isucon/bench run --target https://pipe.u.isucon.local --nameserver 192.168.0.11 --webapp 192.168.0.11 --enable-ssl` from `s3`.
- Began the AGENTS.md sub-agent improvement loop per the user's instruction (~10 iterations, no per-iteration check-in).
- **Iteration 1 (2026-09-23)**: Profiler/Resource-Monitor/App-Understanding/SQL agents dispatched in parallel found almost every hot-path query filtered on an unindexed FK-style column (`livestream_id`/`user_id`/`tag_id`) — only 3 non-PK indexes existed DB-wide pre-change. Added 10 additive indexes (`livestream_tags(livestream_id)`, `livestream_tags(tag_id)`, `icons(user_id)`, `themes(user_id)`, `livecomments(livestream_id)`, `reactions(livestream_id)`, `ng_words(user_id,livestream_id)`, `reservation_slots(start_at,end_at)`, `livestreams(user_id)`, `livecomment_reports(livestream_id)`, `livestream_viewers_history(livestream_id)`) directly on the live DB and mirrored into `webapp/sql/initdb.d/10_schema.sql`. No app code changed. 3 benchmark runs, all `pass=true`: 7475 / 7676 / 7327 (avg 7492.7) vs baseline 3371 — ~2.22x. Commit `b036a4f`, pushed successfully. Latest iteration report: `reports/iterations/iteration-20260923-211905.md`.
- **Iteration 2 (2026-09-23, REJECTED/reverted)**: Rewrote `stats_handler.go`'s N+1 ranking loops (2373/15157 round trips per call) into single `GROUP BY` aggregate queries — per-call latency dropped ~200x (6.2s avg → 30ms) but the benchmark score *regressed* ~26% (avg 6550.5 vs a same-session reverted-state re-measurement of avg 8915), confirmed via A/B (2 runs each direction). Root-cause theory: `main.go`'s `db.SetMaxOpenConns(10)` was an unintentional throttle that the old slow stats handler was implicitly paced by; removing that pacing exposed the 10-connection ceiling as contention elsewhere. Reverted (commit `fb4b562`). Report: `reports/iterations/iteration-20260923-213407.md`.
- **Iteration 3 (2026-09-23)**: Acting on iteration 2's root-cause theory, raised `db.SetMaxOpenConns`/`SetMaxIdleConns` from 10 to 50 (MySQL `max_connections`=151, confirmed ample headroom) — pure Go-side config change, no instance resource change. 3 runs, all `pass=true`: 10798 / 10429 / 11018 (avg 10748.3) vs the prior reverted-state average of 8915 — a further ~20.6% gain, ~3.19x original baseline. Commit `56f73b3`, pushed successfully. Report: `reports/iterations/iteration-20260923-214145.md`.
- **Iteration 4 (2026-09-23)**: Reapplied iteration 2's stats-handler `GROUP BY` fix on top of iteration 3's larger pool. Confirmed the combination is a net win, validating iteration 2's root-cause theory. 3 runs, all `pass=true`: 11818 / 11160 / 12564 (avg 11847.3) vs iteration 3's pool-only average 10748.3 — a further ~10.2% gain, ~3.51x original baseline. Commit `a605b23`, pushed successfully. pprotein artifacts collected against this new baseline. Report: `reports/iterations/iteration-20260923-224700.md`.
- **Iteration 5 (2026-09-23)**: Fresh Profiler/Resource Monitor/App Understanding/SQL agents dispatched in parallel. Resource Monitor's first resource-bound verdict this session: s1 (2 vCPU) CPU-saturated during a live run (idle≈0%, load1 avg 7.38, mysqld ~1 core + app ~0.65 core > 2-core budget) — not memory/disk/connection-bound. Fixed `moderateHandler`'s unscoped `SELECT * FROM livecomments` (no WHERE at all) + per-row DELETE loop, replacing with one scoped batched `DELETE ... WHERE livestream_id=? AND comment LIKE ...` per NG word (worst-latency route: 5.58s avg/11.7s p99 for only 63 calls). 3 runs, all `pass=true`: 12431 / 11703 / 12611 (avg 12248.3) vs iteration 4's 11847.3 — ~3.4% further gain, ~3.63x baseline. Commit `efae626`, pushed successfully. Report: `reports/iterations/iteration-20260923-231000.md`.
- **Iteration 6 (2026-09-23, BOTH CHANGES REJECTED/reverted)**: Tried (1) `CREATE INDEX nametype_index ON isudns.records(name,type)` and (2) persisting `slow_query_log=1`/`long_query_time=0` in the tracked `mysqld.cnf`. Both caused clear, mechanistically-explained regressions, isolated via a step-by-step A/B: (1) speeding up PowerDNS caused the benchmark's own adaptive `DNSAttacker並列数` to escalate from 2 to 3, tripling DNS-attack load and competing for s1's saturated 2 vCPUs (avg ~8700-10200 vs ~11000-12200 without it); (2) `long_query_time=0` (log every query) cost ~20-25% score alone, isolated with DNS-attacker concurrency pinned at 2 (8707 vs 10676/11356). Both reverted; final redeploy-confirmed re-benchmark: 11903, consistent with iteration 5. Durable lessons recorded in `docs/skills/sql-agent.md` (only enable slow-query logging transiently for analysis passes, never during scored runs; don't "fix" PowerDNS speed without checking the benchmark's adaptive DNS-attacker behavior). **Important caveat added 2026-09-23 after a later incident (see below): reverting `long_query_time`/`slow_query_log` in the TRACKED `mysqld.cnf` was too broad a fix — it silently disabled observability for every iteration after this one, since `deploy.sh` re-copies that file over the live config on every deploy. Re-enabled by default later; see the "User-caught observability incident" entry below.** Report: `reports/iterations/iteration-20260923-231800.md`.
- **Iteration 7 (2026-09-23, REJECTED/reverted)**: Added conditional-GET (`If-None-Match`/`ETag`) support to `getIconHandler` per iteration 5's App Understanding Agent finding — correctness verified perfect (curl-tested: unconditional 200, matching-hash 304, mismatched 200), but 3 benchmark runs (11090/11097/11041, avg 11076, unusually tight cluster) showed a consistent ~9.6% regression vs iteration 5/6's ~12000-12200 baseline. Root cause: `sha256.Sum256` computed unconditionally on every one of the route's 18,732 calls/run (highest-count route in the app) on an already CPU-saturated 2-vCPU host, with the benchmark apparently rarely sending `If-None-Match` to offset the cost with 304 savings. Reverted (commit `8aaa23d`), confirmed restored (11902). Report: `reports/iterations/iteration-20260923-232000.md`.
- **Iteration 8 (2026-09-23)**: Fixed `fillLivestreamResponse`'s per-tag N+1 (one `SELECT * FROM tags WHERE id=?` per tag per livestream, one of the highest-*count* query shapes in the app at 12-16k calls/window) by batching into a single `sqlx.In(...)` query and reassembling in original order. 3 runs, all `pass=true`: 12950 / 13135 / 11799 (avg 12628, new session-best single run 13135) vs iteration 5's 12248.3 — ~3-6% further gain, ~3.75x baseline. Commit `8dd8245`, pushed successfully. Report: `reports/iterations/iteration-20260923-233000.md`.
- **Iteration 9 (2026-09-23) — biggest single win of the first session**: Found (via direct code reading, following up on iteration 5's Profiler ranking of `GET livecomment`/`GET reaction` as the #1/#2 highest-total-time routes) that `getLivecommentsHandler`/`getReactionsHandler` each re-fetch and re-fill the *entire* Livestream object (owner, icon, tags) once per returned row, even though every row in a call shares the same `livestream_id`. Fixed by fetching+filling the livestream once per HTTP call (guarded so an empty result list makes zero extra queries, preserving exact prior behavior) and reusing it across all rows via new `fillLivecommentResponseWithLivestream`/`fillReactionResponseWithLivestream` helpers. 3 runs, all `pass=true`: 16727 / 16890 / 17091 (avg 16902.7, new session-best single run 17091) vs iteration 8's 12628 — ~33.9% further gain, ~5.01x baseline. Commit `bf1eff9`, pushed successfully. Report: `reports/iterations/iteration-20260923-234000.md`.
- **Iteration 10 (2026-09-23) — 10th and final iteration of the first session's planned loop**: Applied iteration 9's proven pattern to the one remaining call site with the same bug: `getLivecommentReportsHandler` re-fetched the entire Livestream (owner/icon/tags) once per report row instead of once per call. Fixed via `fillLivecommentReportResponseWithLivestream`, reusing the livestream the handler already fetches for its ownership check. 3 runs, all `pass=true`: 18136 / 16892 / 17246 (avg 17424.7, new session-best single run 18136) vs iteration 9's 16902.7 — ~3.1% further gain, ~5.17x baseline. Commit `45e1aaf`, pushed successfully. Also investigated (but left unfixed, needs human input on desired semantics) a recurring pre-existing 500 on `GET /report` caused by reports pointing at comments already deleted by moderation — not introduced by any change this session. Report: `reports/iterations/iteration-20260923-234500.md`.
- Next candidates (open for a future session): the orphaned-report 500 (needs human input on semantics); a smarter icon-hash approach that only hashes when `If-None-Match` is present; a fresh Profiler/Resource Monitor/SQL Agent dispatch against the ~17400 baseline; an evidence-based app/DB host split onto the idle `s2` given confirmed CPU saturation on `s1` (see the standing-authorization entry below — human approval is no longer required for this specific action).
- Latest app optimization commit (end of first session): `45e1aaf` (isucon13_practice_3 repo on s1). Latest verified benchmark: avg 17424.7 (3 runs, all pass=true), 2026-09-23. Session-best single run: 18136. Latest iteration report: `reports/iterations/iteration-20260923-234500.md`.
- **User-caught observability incident (2026-09-23, after the 10-iteration loop finished)**: the user asked what evidence the loop was actually using, since the nginx access log and MySQL slow log did not look like they were being captured, and separately asked whether netdata's aggregation to the pprotein parent was working as intended. Investigation confirmed **netdata was fine** (verified `mirrored_hosts: [s3, s1, s2]` all `reachable: true` on the `s3` parent, plus live per-second data from `s1` via the API) — no fix needed there. But the user's suspicion about the logs was correct and pointed at two real, independent bugs: (1) `common/etc/nginx/nginx.conf` was never switched off the AMI's stock `combined` log format, so `alp ltsv` (the documented command) was silently returning an empty table for the entire first session — noticed by the Profiler Agent in iteration 1 but only logged as a deferred follow-up, never fixed; later iterations' route-latency claims relied on journalctl-parsed app logs and code reading instead, not alp/httplog as the reports may have implied; (2) MySQL slow-query logging, correctly enabled at initial setup, was silently disabled again for iterations 7-10 because iteration 6 reverted `long_query_time=0` **in the tracked `mysqld.cnf`** (not just live) after finding it cost ~20-25% score, and `deploy.sh` re-copies that tracked file over the live config and restarts MySQL on every single deploy. Both fixed 2026-09-23 (commit `9983294`): nginx now logs LTSV with `reqtime`/`apptime`, `slow_query_log`/`long_query_time=0` are back in the tracked config so they're on by default and survive every deploy, and `deploy.sh` now truncates both log files after every deploy so `alp`/`slp` only ever see "since the last deploy" instead of an ever-growing mix of old runs (the slow log had silently grown to 1.4GB). Verified end-to-end with fresh traffic: `alp ltsv` now returns real non-empty rows with populated `reqtime`, and a live re-benchmark with full logging on scored `pass=true, score=16283` (vs ~17400-18200 with logging off at the same code state — a real but accepted ~7-10% cost, since the user explicitly wants full observability available by default from the start of the environment for humans to reference, not just agents). See `AGENTS.md`'s ISUCON13 section for the full incident writeup and the standing lesson about tracked-config reverts having a much bigger blast radius than live-only fixes.
- Next session should re-run a fresh Profiler/SQL Agent dispatch now that real alp/httplog and slp/slowlog evidence is available again, since the ~17400 baseline's analysis in iterations 7-10 was working without either.
- **Standing authorization granted 2026-09-23**: the user was asked when the loop first started considering an app/DB host split (answer: iteration 5's Resource Monitor Agent, which found `s1` genuinely CPU-saturated during a live run but deliberately deferred acting on it until other levers were exhausted, per the report's own reasoning) and, on hearing that, told Codex it no longer needs to ask before executing an evidence-based split — see `AGENTS.md`'s Safety Boundaries section (moved out of "ask the human before" into its own standing-authorization note). The evidence bar itself (Resource Monitor Agent must show a real benchmark-time bottleneck first) and the absolute no-scale-up rule are both unchanged; only the "pause and ask" step for split *execution* was removed. This makes the app/DB split onto the idle `s2` (flagged as the clear next candidate at the end of the first session) directly actionable in a future session without a check-in.
- **`MILESTONES.md` retired 2026-09-23**: the user pointed out it was originally created to track the (now-abandoned) Python MCP tooling build-out, not ISUCON tuning progress, and asked why it was still being updated. Moved to `archive/MILESTONES.md` (frozen, no longer updated); this file (`reports/summary/isucon13.md`) takes over its narrative-summary role going forward, per the new convention documented in `AGENTS.md`.
- **Also retired 2026-09-23**: `reports/history.jsonl`, `reports/history/*.md`, and `reports/baseline-*.json` — same root cause as `MILESTONES.md` (real output of the now-archived Python MCP tooling, but only ever used for one session on 2026-09-21 and never touched again). Moved to `archive/reports/`.
- **MySQL split executed 2026-09-23** (commit `309c390`), acting on the standing authorization above: moved MySQL (`isupipe` + `isudns` databases) from `s1` to the previously-idle `s2`, keeping `pdns`/`nginx`/`isupipe-go` on `s1`. Mechanics: `s2`'s tracked `mysqld.cnf` now has `bind-address=0.0.0.0` (restricted at the security-group/VPC layer to `192.168.0.0/24`); ran `webapp/sql/initdb.d/00_create_database.sql`+`10_schema.sql` (the git-tracked version, with the iteration-1 indexes) against `s2` to create remote-capable `isucon@'%'`/`isudns@'%'` users and the schema (the AMI's default users were `localhost`-only); pointed `s1`'s `env.sh` and PowerDNS's `gmysql-host.conf` (newly tracked at `common/etc/powerdns/pdns.d/gmysql-host.conf`) at `192.168.0.12`; gave `s2` its own git checkout (same repo/deploy key as `s1`) so a new `common/deploy_db.sh` can deploy MySQL config there, since `common/deploy.sh` (app host) no longer touches `mysql` at all. **Hit and fixed a real gotcha along the way**: `isupipe-go.service`'s unit file had `Requires=mysql.service`, so stopping the now-local-and-unused `mysql.service` on `s1` silently killed the app too via systemd dependency semantics (no obviously-related error in the app's own logs) — removed the `After=`/`Requires=mysql.service` lines. **Result, 3 confirmation benchmark runs, all `pass=true`**: 28773 / 27402 / 27629 (avg **27934.7**) vs the pre-split average of 17424.7 — a further **~1.6x gain**, **~8.29x the original baseline of 3371**, strongly confirming the Resource Monitor Agent's CPU-saturation hypothesis from iteration 5. `isucon_ansible`'s pprotein `targets.json` updated to collect the slowlog from `s2` instead of `s1` (commit `isucon_ansible` `67090a0`). See `AGENTS.md`'s ISUCON13 section and `config/isucon13.yaml` for full mechanics.
- Next candidates for a future session: re-run a fresh Profiler/Resource Monitor/SQL Agent dispatch against the new ~27900 baseline (the `s1`/`s2` resource balance has shifted substantially — check whether `s1` or `s2` is now the tighter constraint); the orphaned-report 500 (needs human input on semantics); a smarter icon-hash approach that only hashes when `If-None-Match` is present.
- **Second tuning session started 2026-09-24, resuming from the 27934.7 baseline.** Fresh benchmark check confirmed the baseline holds (`score=27215, pass=true`, commit `6be450f`). Dispatched Profiler/Resource Monitor/App Understanding/SQL agents in parallel per AGENTS.md's Sub-Agent Execution Protocol. Resource Monitor Agent's key finding: **the bottleneck flipped since the split** — `s1` (app) now has ~20% idle CPU and load1 <1.5/2 cores (headroom), while `s2` (dedicated MySQL) is clearly CPU-bound (idle ~1%, load1 peak 7.56/2 cores, mysqld ~169% CPU). This reframes the rest of this session: prefer changes that reduce `s2`'s own per-query CPU cost, be very cautious about changes that just make `s1`'s work cheaper (see the rejected iteration below for why).
- **Iteration 1 (2026-09-24) — one sub-change rejected via A/B bisection, two accepted.** App Understanding Agent recommended a low-risk fix for the long-standing pre-existing `GET /livestream/:id/report` 500 (orphaned `livecomment_reports` rows pointing at moderation-deleted `livecomments`, since `moderateHandler` never cleans up reports): skip the orphaned row instead of failing the whole request. Implemented and **accepted**. Separately, tried precomputing/storing `icons.icon_hash` at write time so `fillUserResponse` (called on nearly every hot path) could select a small hash column instead of fetching+hashing the full icon image BLOB on every call — **this looked correct and was independently confirmed ~8x cheaper per query by a follow-up SQL Agent dispatch, but net-regressed the benchmark score** (15880/17682 vs a same-session A/B-reverted 25090) and was **rejected/reverted** (commit `17691ec`). Working theory, consistent with the Resource Monitor Agent's fresh finding above: `s1` had CPU headroom, not `s2`, so making `s1`'s work cheaper just let the app submit work to the already-CPU-saturated `s2` faster without reducing `s2`'s own per-query cost, worsening contention (SQL Agent's follow-up data showed elevated `ROLLBACK` counts and a shared ~57s max-latency spike across unrelated query shapes during the regressed runs). This is the same "removing implicit pacing exposes contention elsewhere" lesson as the first session's iteration 2, now confirmed to generalize across the DB split. Also added `ng_words(livestream_id)` index (SQL Agent found a full table scan on `moderateHandler`'s NG-word re-check query, `EXPLAIN` confirmed `type=ALL`) — a safe pattern since it reduces `s2`'s own per-query CPU without changing app-side call volume. Final accepted state (commit `b1e926d`): 3 runs, all `pass=true`: 25963 / 27127 / 26383 (avg **26491.7**) — roughly flat vs. the 27934.7 recorded baseline (today's session appears to have a slightly lower noise floor overall: the A/B leg using fully-unmodified old code also scored only 25090 in the same session, so the accepted changes scored *above* that same-session control). Report: `reports/iterations/iteration-20260923-152055.md`.
- **Second session's iteration 2 in progress was interrupted by an operator-side rate limit** (session token limit hit mid-iteration), leaving the loop paused after iteration 1's commit `b1e926d`.
- **Environment lost and restored, 2026-09-24**: the user accidentally deleted the CloudFormation stack overnight. Recreated from `isucon_cf_provisioning/isucon13/cf-template-isucon13.yaml`; new public IPs (`s1`=`35.79.95.206`, `s2`=`13.193.230.57`, `s3`=`35.76.34.10`, private IPs unchanged). Re-ran `general`/`pprotein` ansible roles, gave `s1`/`s2` fresh git checkouts at `b1e926d` (nothing was lost — all tuning progress was already pushed to GitHub), reapplied `s2`'s MySQL schema+remote users (`DROP DATABASE`/`CREATE DATABASE` + `10_schema.sql`, which already contains every index added across both sessions, including the `ng_words(livestream_id)` one from iteration 1 above + `00_create_database.sql`), repointed `env.sh`/PowerDNS `gmysql-host.conf` at `192.168.0.12` (unchanged private IP), stopped local `mysql` on `s1` again. Verified with a fresh benchmark run: `pass=true`, `score=27750` — consistent with the pre-deletion range, confirming nothing was actually lost. Resuming the second session's loop from here (iteration 2 onward).
- **Second session, iteration 2 (2026-09-24) — one neutral change kept, one hypothesis rejected, and a major environment-noise discovery.** Fresh 4-agent dispatch reconfirmed `s2` (MySQL) CPU-bound (idle ~0%, load1 up to 8.2) and `s1` with headroom (44% idle floor). App Understanding found a real unfixed N+1 in `searchLivestreamsHandler`'s tag branch (one `SELECT * FROM livestreams WHERE id=?` per row), which the Profiler independently ranked as the top route by total time; batched it (commit `eca2362`) but a clean A/B showed no measurable score change (25643 vs control 26023) — kept as a correct, strictly-fewer-queries neutral change. Re-tested the `isudns.records(name,type)` index now that `s1` has headroom: rejected again (24092 vs ~25800; benchmark DNS attacker parallelism climbs to 4) — the "don't speed up PowerDNS" lesson holds under the split topology. **Big gotcha found**: freshly recreated instances run Ubuntu's `unattended-upgrade` shortly after boot (92% of a core on `s1`; a 25+ minute full upgrade of mysql-server/systemd/kernel/nginx on `s2`), silently corrupting benchmark numbers (scores of 16086/19230/24473 seen before it was noticed). Disabled `unattended-upgrades.service` + `apt-daily(.upgrade).timer` on `s1`/`s2`; stopped the running upgrade with a graceful SIGTERM (never `kill -9` mid-dpkg on the DB host); `dpkg --audit` clean. Report: `reports/iterations/iteration-20260924-010000.md`.
- **Second session, iteration 3 (2026-09-24) — ACCEPTED, new best**: MySQL commit-path tuning in the tracked `mysqld.cnf` (`disable_log_bin`, `innodb_flush_log_at_trx_commit=2`, `innodb_flush_method=O_DIRECT`; slow-query logging left on). Live values were stock (`log_bin=1`, `sync_binlog=1`, flush=1 → ~2 fsyncs per commit; `COMMIT` was the #2 SQL cost). 3 runs, all `pass=true`: 30057 / 28801 / 29048 (avg **29302**) vs clean same-session control avg 25833 — **~+13.4%**, worst treatment run above best control run. Data still survives a clean reboot (contest rule). Commit `587f9e0`. Report: `reports/iterations/iteration-20260924-012500.md`.
- **Second session, iteration 4 (2026-09-24) — neutral, kept**: `performance_schema=OFF`, 1G InnoDB buffer pool/redo (commit `a3386ee`): 28215/29816/29011 (avg 29014) vs 29302 — noise. Report: `reports/iterations/iteration-20260924-014500.md`. A netdata per-interval capture of this run showed both hosts now near saturation (s2 ~100% busy, s1 ~87% incl. 12% softirq) and pprotein's pprof showed `crypto/sha256` = 35.6% of the app's CPU (icon hashing in `fillUserResponse`).
- **Second session, iteration 5 (2026-09-24) — ACCEPTED, new best, ~+27.8%**: in-process per-user icon-hash cache (`iconHashCache`; `postIconHandler` stores after commit, readers `LoadOrStore`, `initialize` clears) removes the per-call icon BLOB fetch + sha256. 37710/36375/37117 (avg **37067**) vs 29014, all pass=true. Note this is the same underlying idea as session-2-iteration-1's rejected `icons.icon_hash` DB-column variant, but that one still issued a query per call and ran while s1 had large headroom and s2 was the sole bottleneck; after iteration 3 relieved s2 the balance changed and the cache (which eliminates the query, not just the hash) is a clear win. Lesson: an app-side optimization rejected under one host balance can become the right move after the constraint moves — re-test rather than treating a rejection as permanent. Commit `3b27cbc`. Report: `reports/iterations/iteration-20260924-015500.md`.
- **Second session, iteration 6 (2026-09-24) — ACCEPTED, new best, ~+65%**: fresh Profiler + SQL agents (run alone after a bench, on s3 artifacts) agreed the biggest remaining isupipe lever was ~90k `SELECT * FROM themes WHERE user_id=?` and ~90k `SELECT * FROM users WHERE id=?` per run. Grep confirmed users/themes are insert-only (register is the only writer), so added immutable in-process caches (`userModelCache`, `themeModelCache`, cleared by `initialize`). 63046/60123/60945 (avg **61371**) vs 37067, all pass=true, no new error categories. Commit `120aea8`. Report: `reports/iterations/iteration-20260924-021500.md`. Pattern across iterations 5-6: with the DB commit path cheap and both hosts near saturation, removing whole queries via caches of immutable data pays far more than making queries cheaper.
- **Second session, iteration 7 (2026-09-24) — ACCEPTED, new best, ~+13%**: `reserveLivestreamHandler`'s `reservation_slots` `FOR UPDATE`/`UPDATE` did full-table scans (8773 rows, ~1.67M rows examined per window, whole-table locks) because only `start_at >= ?` could seek. Added the logically implied `AND start_at < end` bound (range scan on `idx_start_end`, identical matched rows) and replaced a redundant per-slot re-query (1796 per window) with the already-locked `slot.Slot`. 69037/69740/69210 (avg **69329**) vs 61371, all pass=true. Commit `167a643`. Report: `reports/iterations/iteration-20260924-023500.md`.
